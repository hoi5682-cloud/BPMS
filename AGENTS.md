# AGENTS.md — BPMS 프로젝트 Codex 진입점

> 이 폴더는 워크스페이스 안에 있지만 자체 `.git`을 가진 독립 저장소다.

## 시작할 때

1. `README.md`로 프로젝트 목적과 파일 구조를 확인한다.
2. `progress.md`에서 현재 단계, 확정된 스타일, 사용자 확인이 필요한 질문을 확인한다.
3. `CLAUDE.md`에서 BPMN 도형 어휘와 Figma 작업 규칙을 확인한다.
4. `git status`, `git diff`, 최근 관련 커밋으로 실제 상태를 확인한다.

## 작업 규칙

- 원본 프로세스 사실은 `제목 없는 다이어그램.drawio`와 `_analysis/drawio_parsed.json`을 근거로 삼는다.
- 확정된 E안의 도형 어휘와 레인 컬러+시스템 배지 규칙을 유지한다.
- `progress.md`에 남은 사용자 확인 질문은 답을 받기 전 임의로 확정하지 않는다.
- 최종 산출물은 Figma 파일이며 저장소의 Python 코드는 drawio 분석 보조 도구다.
- `.claude/skills/`와 `.claude/agents/`는 Claude Code 전용이다. Codex는 현재 환경에 제공된 별도 스킬과 도구를 사용한다.
- 이 폴더의 커밋과 push는 BPMS 저장소에서 별도로 처리한다. 상위 워크스페이스 Git에는 포함되지 않는다.
- 실질적인 변경을 다른 도구나 세션으로 넘길 때만 `progress.md`의 현재 상태와 로그를 갱신한다.

## 검증

- 파서 변경 시 실제 drawio 파일로 실행하고 생성 결과를 확인한다.
- 다이어그램 변경 시 원본의 순서·조건분기 보존 여부와 BPMN 표기 일관성을 확인한다.
- Figma 공유나 배포처럼 외부 상태가 바뀌는 작업은 사용자 요청 범위 안에서만 수행한다.
